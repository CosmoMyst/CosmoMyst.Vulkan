{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/Vulkan.Engine/Vulkan.Engine.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}